#include "main.h"
#include <string.h>
/**
 * _strlen - Calculates the length of a string.
 *
 * This file contains a C program that defines a function to calculate the
 * length (number of characters) of a string. The function takes a pointer to a
 * null-terminated string as a parameter and returns the length of the string.
 *
 * @s A pointer to a null-terminated string.
 */
int _strlen(char *s)
{
return (strlen(s));
}
